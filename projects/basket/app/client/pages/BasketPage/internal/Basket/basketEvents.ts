export const BasketEvents = {
  EmptyBasket: 'basket.empty',
  BasketUpdate: 'basket.update',
  RemovePackage: 'basket.removePackage',
  UndoRemovePackage: 'basket.undoRemovePackage',
  RemoveAddon: 'basket.removeAddon',
  UndoRemoveAddon: 'basket.undoRemoveAddon',
  RemoveVoucher: 'basket.removeVoucher',
  ToggleModal: 'basket.toggleModal',
  FireAnalytics: 'basket.fireAnalytics',
}
